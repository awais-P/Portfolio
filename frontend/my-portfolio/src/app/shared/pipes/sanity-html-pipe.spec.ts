import { SanityHtmlPipe } from './sanity-html-pipe';

describe('SanityHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new SanityHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
